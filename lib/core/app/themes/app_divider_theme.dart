import 'package:flutter/material.dart';
import 'package:mustye/core/app/resources/colors.dart';

class AppDividerTheme {
  const AppDividerTheme._();

  static const light = DividerThemeData(color: Colours.grey600);

  static const dark = DividerThemeData(color: Colours.grey300);
}
